# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USER=hodler
DB_PASSWORD=123123
DB_NAME=hodler

# Server Configuration
PORT=3000
NODE_ENV=development
PING_INTERVAL=10000
PING_TIMEOUT=30000
NETWORK_NAME=hodler-network
CRAWLER_IMAGE=hodlertrack-crawler:latest

# Database Migration (optional)
# Set to true to automatically run migrations on startup if schema is missing
AUTO_MIGRATE=false